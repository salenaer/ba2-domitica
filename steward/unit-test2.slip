;-----------------------------------------------------------------------------------------------------------------------
;------------------------------------------------------ unit-test ------------------------------------------------------
;-----------------------------------------------------------------------------------------------------------------------

(load "protocol")
(load "basic")
(load "device")
(load "simulation-steward")
(load "xbee-simulation")

(define steward (make-steward "steward1" "localhost"))

(define (loop)
  (while (not (eq? (read) "stop")) (steward)))

(loop)

